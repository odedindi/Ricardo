// ======================= logo ===========================
import logo from '../../assets/logo.svg'
// ========================================================

const Logo = () => (

    <img 
        src={ logo }
        alt="Ricardo's Logo"
        width='75rem'
    />
);

export default Logo;
